# Sanitized machine-local tunables for ~/.config/oes/local.env.
# Runtime identities, endpoints, ports, credentials and certificate paths are launcher-owned
# dynamic bindings and are deliberately not accepted from this file.
OES_RUNTIME_CONCURRENCY=2
OES_RUNTIME_LOG_LEVEL=info
