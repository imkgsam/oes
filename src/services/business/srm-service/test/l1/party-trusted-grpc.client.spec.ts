import { SrmPartyTrustedGrpcExecutionProducer } from '../../src/infrastructure/adapters/srm-party-trusted-grpc-execution.producer'
const source = { accessor: {}, run: async (callback: () => Promise<unknown>) => callback() }
describe('SRM Party trusted execution', () => { it('fails closed without traceparent', async () => { process.env.SRM_PARTY_MACHINE_PRINCIPAL_ID='machine-srm'; const producer=new SrmPartyTrustedGrpcExecutionProducer(source as never,{} as never); await expect(producer.createMetadata('party.internal.get_tenant_party_by_id','request-1')).rejects.toThrow('PARTY_CALLER_EXECUTION_CONTEXT_REQUIRED') }); it('fails closed without foundation configuration', async()=>{delete process.env.AUTH_EXECUTION_ISSUER;process.env.SRM_PARTY_MACHINE_PRINCIPAL_ID='machine-srm';const producer=new SrmPartyTrustedGrpcExecutionProducer(source as never,{} as never);await expect(producer.createMetadata('party.internal.get_tenant_party_by_id','request-1','00-0123456789abcdef0123456789abcdef-0123456789abcdef-01')).rejects.toThrow('PARTY_CALLER_FOUNDATION_UNAVAILABLE')}) })
