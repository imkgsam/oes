import { SrmPartyTrustedGrpcExecutionProducer } from '../../src/infrastructure/adapters/srm-party-trusted-grpc-execution.producer'
describe('SRM Party trusted execution', () => { it('fails closed without a W3C traceparent', async () => { process.env.AUTH_EXECUTION_ISSUER='https://issuer.example'; process.env.OES_WORKLOAD_SPIFFE_ID='spiffe://oes/srm-service'; process.env.SRM_PARTY_MACHINE_PRINCIPAL_ID='machine-srm'; const producer=new SrmPartyTrustedGrpcExecutionProducer({accessor:{}} as never,{} as never); await expect(producer.createMetadata('party.internal.get_tenant_party_by_id','request-1')).rejects.toThrow('PARTY_CALLER_EXECUTION_CONTEXT_REQUIRED') }) })
