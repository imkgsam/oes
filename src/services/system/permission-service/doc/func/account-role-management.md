# 账号角色管理

更新时间：2026-03-19 10:19:56 +08:00

## 目标

提供账号与角色绑定的完整管理闭环，并支持租户后台常见的“checkbox list 角色设置页面”。

## 设计决策

- 当前页面交互采用“全量同步角色集合”语义，而不是多次增量授予/撤销。
- `SetAccountRoles` 只允许设置当前租户的 `TENANT_INSTANCE`。
- 撤销不存在的账号角色绑定按幂等成功处理。
- 当前“仅租户管理员可调用”仍依赖上层鉴权，后续需要在服务内补足保护。

## 功能清单

| 功能编号 | 功能项 | 允许调用服务 | 允许操作者 | 优先级 | 状态 | 备注 |
|---|---|---|---|---|---|---|
| 4.3.1 | 查看账号持有的角色 | `gateway` | 租户管理员 | P0 | 已实现 | 按 `accountId + tenantId` 查询 |
| 4.3.2 | 给账号授予角色 | `gateway` | 租户管理员 | P0 | 已实现 | 单条绑定 |
| 4.3.3 | 撤销账号角色 | `gateway` | 租户管理员 | P0 | 已实现 | 不存在绑定时幂等成功 |
| 4.3.4 | 获取账号角色选择列表 | `gateway` | 租户管理员 | P0 | 已实现 | 返回 `availableRoles[] + selectedRoleIds[]` |
| 4.3.5 | 设置账号角色集合 | `gateway` | 租户管理员 | P0 | 已实现 | checkbox list 页面保存入口 |
| 4.3.6 | 账号角色生效时间 | `gateway` | 租户管理员 | P1 | 未开始 | 支持临时授权、延期、过期 |

## 分片实施建议

1. 为现有账号角色管理接口补服务内保护
2. 收敛操作者上下文校验
3. 再推进账号角色生效时间
