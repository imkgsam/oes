import { TenantOrgPartyTrustedGrpcExecutionProducer } from '../../src/infrastructure/adapters/tenant-org-party-trusted-grpc-execution.producer'
describe('TenantOrg Party trusted execution', () => { it('fails closed without a W3C traceparent', async () => { process.env.AUTH_EXECUTION_ISSUER='https://issuer.example'; process.env.OES_WORKLOAD_SPIFFE_ID='spiffe://oes/tenant-org-service'; process.env.TENANT_ORG_PARTY_MACHINE_PRINCIPAL_ID='machine-tenant-org'; const producer=new TenantOrgPartyTrustedGrpcExecutionProducer({accessor:{}} as never,{} as never); await expect(producer.createMetadata('party.internal.register_tenant_party','request-1')).rejects.toThrow('PARTY_CALLER_EXECUTION_CONTEXT_REQUIRED') }) })
