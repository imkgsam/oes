# 1.2 按个人手机查询用户记录

更新时间：2026-03-23 19:58:00 +08:00

## 本次目标

实现 `identity-service` 的个人手机查询闭环：按个人手机查询 `User`。

## 主要调整

- 为 `identity_service` gRPC 合约新增 `GetUserByPhone`
- 新增 `GetUserByPhoneQuery` 与 handler
- `UserRepository` 与 Prisma 实现增加按手机号查询
- gRPC controller 接入 `getUserByPhone`
- 当前继续复用历史 schema 中的 `User.phone` 承接“个人手机”语义

## 备注

本次不调整 `schema.prisma` 字段命名，只完成查询闭环。
