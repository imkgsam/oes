import { IdentityPartyTrustedGrpcExecutionProducer } from '../../src/infrastructure/adaptors/identity-party-trusted-grpc-execution.producer'
const source = { accessor: {}, run: async (callback: () => Promise<unknown>) => callback() }
describe('Identity Party trusted execution', () => { it('fails closed without traceparent', async () => { process.env.IDENTITY_PARTY_MACHINE_PRINCIPAL_ID='machine-identity'; const producer=new IdentityPartyTrustedGrpcExecutionProducer(source as never,{} as never); await expect(producer.createMetadata('party.internal.register_tenant_party','request-1')).rejects.toThrow('PARTY_CALLER_EXECUTION_CONTEXT_REQUIRED') }); it('fails closed without foundation configuration', async()=>{delete process.env.AUTH_EXECUTION_ISSUER;process.env.IDENTITY_PARTY_MACHINE_PRINCIPAL_ID='machine-identity';const producer=new IdentityPartyTrustedGrpcExecutionProducer(source as never,{} as never);await expect(producer.createMetadata('party.internal.register_tenant_party','request-1','00-0123456789abcdef0123456789abcdef-0123456789abcdef-01')).rejects.toThrow('PARTY_CALLER_FOUNDATION_UNAVAILABLE')}) })
