import { IdentityPartyTrustedGrpcExecutionProducer } from '../../src/infrastructure/adaptors/identity-party-trusted-grpc-execution.producer'
describe('Identity Party trusted execution', () => { it('fails closed without a W3C traceparent', async () => { process.env.AUTH_EXECUTION_ISSUER='https://issuer.example'; process.env.OES_WORKLOAD_SPIFFE_ID='spiffe://oes/identity-service'; process.env.IDENTITY_PARTY_MACHINE_PRINCIPAL_ID='machine-identity'; const producer=new IdentityPartyTrustedGrpcExecutionProducer({accessor:{}} as never,{} as never); await expect(producer.createMetadata('party.internal.register_tenant_party','request-1')).rejects.toThrow('PARTY_CALLER_EXECUTION_CONTEXT_REQUIRED') }) })
