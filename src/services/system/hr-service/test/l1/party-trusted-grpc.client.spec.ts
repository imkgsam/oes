import { HrPartyTrustedGrpcExecutionProducer } from '../../src/infrastructure/adapters/hr-party-trusted-grpc-execution.producer'
describe('HR Party trusted execution', () => { it('fails closed without a W3C traceparent', async () => { process.env.AUTH_EXECUTION_ISSUER='https://issuer.example'; process.env.OES_WORKLOAD_SPIFFE_ID='spiffe://oes/hr-service'; process.env.HR_PARTY_MACHINE_PRINCIPAL_ID='machine-hr'; const producer=new HrPartyTrustedGrpcExecutionProducer({accessor:{}} as never,{} as never); await expect(producer.createMetadata('party.internal.register_tenant_party','request-1')).rejects.toThrow('PARTY_CALLER_EXECUTION_CONTEXT_REQUIRED') }) })
