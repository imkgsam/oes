import { Test } from '@nestjs/testing'
import { HrTrustedExecutionModule } from '../../src/modules/hr-trusted-execution.module'
import { HrPartyTrustedGrpcClient } from '../../src/infrastructure/adapters/party-trusted-grpc.client'
import { HrPartyTrustedGrpcExecutionProducer } from '../../src/infrastructure/adapters/hr-party-trusted-grpc-execution.producer'
describe('HR Party trusted execution', () => { it('resolves dedicated DI providers', async () => { const module=await Test.createTestingModule({imports:[HrTrustedExecutionModule]}).compile();expect(module.get(HrPartyTrustedGrpcClient)).toBeDefined();expect(module.get(HrPartyTrustedGrpcExecutionProducer)).toBeDefined();await module.close() }); it('fails closed without traceparent', async()=>{process.env.HR_PARTY_MACHINE_PRINCIPAL_ID='machine-hr';const p=new HrPartyTrustedGrpcExecutionProducer({accessor:{}} as never,{} as never);await expect(p.createMetadata('party.internal.register_tenant_party','request-1')).rejects.toThrow('PARTY_CALLER_EXECUTION_CONTEXT_REQUIRED')}) })
