/** Declares the non-role-grantable internal Asset URL projection authority. */
export const ASSET_INTERNAL_PERMISSION_CODES = Object.freeze({
  AVATAR_RESOLVE_PUBLIC_URL: 'asset.internal.avatar.resolve_public_url'
})
