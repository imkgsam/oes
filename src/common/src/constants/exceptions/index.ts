import { SECURITY_EXCEPTIONS } from './security'
import { VALIDATION_EXCEPTIONS } from './validation.exceptions'
import { SYSTEM_EXCEPTIONS } from './system.exceptions'
import { RUNTIME_ERRORS } from './runtime.exceptions'
import { INTEGRATION_EXCEPTIONS } from './integration'

export const GLOBAL_EXCEPTIONS = {
  SECURITY_EXCEPTIONS,
  VALIDATION_EXCEPTIONS,
  SYSTEM_EXCEPTIONS,
  RUNTIME_ERRORS,
  INTEGRATION_EXCEPTIONS
}
