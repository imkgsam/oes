import { DB_EXCEPTIONS } from './db'
import { REDIS_EXCEPTIONS } from './redis'
import { RPC_EXCEPTIONS } from './rpc'

export const INTEGRATION_EXCEPTIONS = {
  DB_EXCEPTIONS,
  REDIS_EXCEPTIONS,
  RPC_EXCEPTIONS
}
