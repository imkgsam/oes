# Runs APISIX as a data plane backed only by the versioned standalone route document.
deployment:
  role: data_plane
  role_data_plane:
    config_provider: yaml

apisix:
  node_listen: 9080
  enable_admin: false

nginx_config:
  error_log_level: warn
