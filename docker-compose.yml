include:
  - path: docker-compose.infra.yml

x-service-defaults: &service-defaults
  labels:
    oes.local.owner: ${OES_TASK_KEY:?run pnpm env:bootstrap}
    oes.local.scope: infrastructure-databases
  networks: [oes_network]
  depends_on:
    postgres: { condition: service_healthy }
    redis: { condition: service_healthy }
    otel-collector: { condition: service_started }
  restart: unless-stopped

x-service-environment: &service-environment
  NODE_ENV: development
  GRPC_LISTEN_HOST: 0.0.0.0
  REDIS_HOST: redis
  REDIS_PORT: 6379
  NACOS_SERVER: nacos:8848
  OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4318
  AUTH_SERVICE_HOST: auth-service
  AUTH_SERVICE_PORT: 50050
  AUTH_EXECUTION_ISSUER: https://issuer.local.oes.internal

services:
  grpc-trust-bootstrap:
    image: alpine:3.21
    labels:
      oes.local.owner: ${OES_TASK_KEY:?run pnpm env:bootstrap}
      oes.local.scope: infrastructure-databases
    command: ['/bin/sh', '-ec', 'apk add --no-cache bash openssl && exec /scripts/bootstrap-local-trust.sh --output /trust']
    environment:
      OES_TRUST_ENV: local
      OES_TRUST_OUTPUT_DIRECTORY: /trust
      OES_ROTATION_CHECK_INTERVAL_SECONDS: 300
    volumes:
      - ./docker/grpc-trust:/scripts:ro
      - grpc_trust_runtime:/trust
    networks: [oes_network]

  grpc-trust-rotator:
    image: alpine:3.21
    labels:
      oes.local.owner: ${OES_TASK_KEY:?run pnpm env:bootstrap}
      oes.local.scope: infrastructure-databases
    command: ['/bin/sh', '-ec', 'apk add --no-cache bash openssl && exec /scripts/rotate-local-trust.sh']
    depends_on:
      grpc-trust-bootstrap: { condition: service_completed_successfully }
    environment:
      OES_TRUST_ENV: local
      OES_TRUST_OUTPUT_DIRECTORY: /trust
      OES_ROTATION_CHECK_INTERVAL_SECONDS: 300
    restart: unless-stopped
    volumes:
      - ./docker/grpc-trust:/scripts:ro
      - grpc_trust_runtime:/trust
    networks: [oes_network]

  api-gateway:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.api-gateway
    environment:
      <<: *service-environment
      MODULE_NAME: api-gateway
      SERVICE_PORT: 9101
      AUTH_SERVICE_HOST: auth-service
      AUTH_SERVICE_PORT: 50050
      IDENTITY_SERVICE_HOST: identity-service
      IDENTITY_SERVICE_PORT: 50052
      PERMISSION_SERVICE_HOST: permission-service
      PERMISSION_SERVICE_PORT: 50051
      PARTY_SERVICE_HOST: party-service
      PARTY_SERVICE_PORT: 50053
      TENANT_ORG_SERVICE_HOST: tenant-org-service
      TENANT_ORG_SERVICE_PORT: 50054
      HR_SERVICE_HOST: hr-service
      HR_SERVICE_PORT: 50055
      ASSET_SERVICE_HOST: asset-service
      ASSET_SERVICE_PORT: 50056
      TERMINAL_DEVICE_SERVICE_HOST: terminal-device-service
      TERMINAL_DEVICE_SERVICE_PORT: 50057
      ITEM_MASTER_SERVICE_HOST: item-master-service
      ITEM_MASTER_SERVICE_PORT: 50058
      SALES_SERVICE_HOST: sales-service
      SALES_SERVICE_PORT: 50059
      CRM_SERVICE_HOST: crm-service
      CRM_SERVICE_PORT: 50060
      SRM_SERVICE_HOST: srm-service
      SRM_SERVICE_PORT: 50061
      PROCUREMENT_SERVICE_HOST: procurement-service
      PROCUREMENT_SERVICE_PORT: 50062
      FINANCE_SERVICE_HOST: finance-service
      FINANCE_SERVICE_PORT: 50063
      WMS_SERVICE_HOST: wms-service
      WMS_SERVICE_PORT: 50064
      MES_SERVICE_HOST: mes-service
      MES_SERVICE_PORT: 50065
      PUBLIC_ENTRY_SERVICE_HOST: public-entry-service
      PUBLIC_ENTRY_SERVICE_PORT: 50067
      COLLABORATION_SERVICE_HOST: collaboration-service
      COLLABORATION_SERVICE_PORT: 50068
      SITE_SERVICE_HOST: site-service
      SITE_SERVICE_PORT: 50069
      BROWSER_ACTIVITY_SERVICE_HOST: browser-activity-service
      BROWSER_ACTIVITY_SERVICE_PORT: 50070
    ports: ['127.0.0.1::9101']

  asset-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: asset-service
        SERVICE_PATH: src/services/system/asset-service
    environment:
      <<: *service-environment
      MODULE_NAME: asset-service
      DATABASE_URL: ${OES_DB_ASSET_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50056
      ASSET_S3_ENDPOINT: http://minio:9000
      ASSET_S3_BUCKET: ${MINIO_BUCKET:-oes-local-assets}
      ASSET_S3_ACCESS_KEY_ID: ${MINIO_ROOT_USER:?run pnpm db:config}
      ASSET_S3_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD:?run pnpm db:config}
    expose: ['50056']

  auth-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: auth-service
        SERVICE_PATH: src/services/system/auth-service
    environment:
      <<: *service-environment
      MODULE_NAME: auth-service
      DATABASE_URL: ${OES_DB_AUTH_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50050
      AUTH_HTTP_PORT: 50051
    expose: ['50050']

  browser-activity-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: browser-activity-service
        SERVICE_PATH: src/services/system/browser-activity-service
    environment:
      <<: *service-environment
      MODULE_NAME: browser-activity-service
      DATABASE_URL: ${OES_DB_BROWSER_ACTIVITY_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50070
    expose: ['50070']

  collaboration-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: collaboration-service
        SERVICE_PATH: src/services/system/collaboration-service
    environment:
      <<: *service-environment
      MODULE_NAME: collaboration-service
      DATABASE_URL: ${OES_DB_COLLABORATION_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50068
      NATS_COLLABORATION_USER: ${NATS_COLLABORATION_USER:?run pnpm db:config}
      NATS_COLLABORATION_PASSWORD: ${NATS_COLLABORATION_PASSWORD:?run pnpm db:config}
      COLLABORATION_DATABASE_URL: ${OES_DB_COLLABORATION_SERVICE_URL:?run pnpm db:config}
    expose: ['50068']

  hr-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: hr-service
        SERVICE_PATH: src/services/system/hr-service
    environment:
      <<: *service-environment
      MODULE_NAME: hr-service
      DATABASE_URL: ${OES_DB_HR_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50055
    expose: ['50055']

  identity-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: identity-service
        SERVICE_PATH: src/services/system/identity-service
    environment:
      <<: *service-environment
      MODULE_NAME: identity-service
      DATABASE_URL: ${OES_DB_IDENTITY_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50052
    expose: ['50052']

  item-master-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: item-master-service
        SERVICE_PATH: src/services/system/item-master-service
    environment:
      <<: *service-environment
      MODULE_NAME: item-master-service
      DATABASE_URL: ${OES_DB_ITEM_MASTER_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50058
    expose: ['50058']

  notification-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: notification-service
        SERVICE_PATH: src/services/system/notification-service
    environment:
      <<: *service-environment
      MODULE_NAME: notification-service
      DATABASE_URL: ${OES_DB_NOTIFICATION_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50066
      NATS_NOTIFICATION_USER: ${NATS_NOTIFICATION_USER:?run pnpm db:config}
      NATS_NOTIFICATION_PASSWORD: ${NATS_NOTIFICATION_PASSWORD:?run pnpm db:config}
    expose: ['50066']

  party-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: party-service
        SERVICE_PATH: src/services/system/party-service
    environment:
      <<: *service-environment
      MODULE_NAME: party-service
      DATABASE_URL: ${OES_DB_PARTY_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50053
    expose: ['50053']

  permission-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: permission-service
        SERVICE_PATH: src/services/system/permission-service
    environment:
      <<: *service-environment
      MODULE_NAME: permission-service
      DATABASE_URL: ${OES_DB_PERMISSION_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50051
    expose: ['50051']

  public-entry-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: public-entry-service
        SERVICE_PATH: src/services/system/public-entry-service
    environment:
      <<: *service-environment
      MODULE_NAME: public-entry-service
      DATABASE_URL: ${OES_DB_PUBLIC_ENTRY_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50067
    expose: ['50067']

  site-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: site-service
        SERVICE_PATH: src/services/system/site-service
    environment:
      <<: *service-environment
      MODULE_NAME: site-service
      DATABASE_URL: ${OES_DB_SITE_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50069
    expose: ['50069']

  tenant-org-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: tenant-org-service
        SERVICE_PATH: src/services/system/tenant-org-service
    environment:
      <<: *service-environment
      MODULE_NAME: tenant-org-service
      DATABASE_URL: ${OES_DB_TENANT_ORG_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50054
    expose: ['50054']

  terminal-device-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: terminal-device-service
        SERVICE_PATH: src/services/system/terminal-device-service
    environment:
      <<: *service-environment
      MODULE_NAME: terminal-device-service
      DATABASE_URL: ${OES_DB_TERMINAL_DEVICE_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50057
    expose: ['50057']

  crm-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: crm-service
        SERVICE_PATH: src/services/business/crm-service
    environment:
      <<: *service-environment
      MODULE_NAME: crm-service
      DATABASE_URL: ${OES_DB_CRM_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50060
    expose: ['50060']

  finance-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: finance-service
        SERVICE_PATH: src/services/business/finance-service
    environment:
      <<: *service-environment
      MODULE_NAME: finance-service
      DATABASE_URL: ${OES_DB_FINANCE_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50063
    expose: ['50063']

  mes-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: mes-service
        SERVICE_PATH: src/services/business/mes-service
    environment:
      <<: *service-environment
      MODULE_NAME: mes-service
      DATABASE_URL: ${OES_DB_MES_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50065
    expose: ['50065']

  procurement-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: procurement-service
        SERVICE_PATH: src/services/business/procurement-service
    environment:
      <<: *service-environment
      MODULE_NAME: procurement-service
      DATABASE_URL: ${OES_DB_PROCUREMENT_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50062
    expose: ['50062']

  sales-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: sales-service
        SERVICE_PATH: src/services/business/sales-service
    environment:
      <<: *service-environment
      MODULE_NAME: sales-service
      DATABASE_URL: ${OES_DB_SALES_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50059
    expose: ['50059']

  srm-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: srm-service
        SERVICE_PATH: src/services/business/srm-service
    environment:
      <<: *service-environment
      MODULE_NAME: srm-service
      DATABASE_URL: ${OES_DB_SRM_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50060
    expose: ['50060']

  wms-service:
    <<: *service-defaults
    build:
      context: .
      dockerfile: docker/Dockerfile.service
      args:
        PACKAGE_NAME: wms-service
        SERVICE_PATH: src/services/business/wms-service
    environment:
      <<: *service-environment
      MODULE_NAME: wms-service
      DATABASE_URL: ${OES_DB_WMS_SERVICE_URL:?run pnpm db:config}
      GRPC_LISTEN_PORT: 50064
    expose: ['50064']

volumes:
  grpc_trust_runtime:
    labels:
      oes.local.owner: ${OES_TASK_KEY:?run pnpm env:bootstrap}
      oes.local.scope: infrastructure-databases
