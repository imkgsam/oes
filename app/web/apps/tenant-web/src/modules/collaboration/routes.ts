import type { RouteRecordRaw } from 'vue-router';

// Collaboration is reserved as a first-class tenant-web module.
// Concrete pages will be introduced once BFF contracts are ready.
const collaborationRoutes: RouteRecordRaw[] = [];

export { collaborationRoutes };
export default collaborationRoutes;
